@model IEnumerable<ProjetHeritageGroupe8.Models.Ecole>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Regles)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Regles.RegleID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EcoleID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EcoleID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EcoleID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
